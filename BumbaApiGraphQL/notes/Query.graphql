query GetAllUsers {
  users {
    nodes {
      id
      name
    }
  }
}

query GetUserById($userId: Int!) {
  me(id: $userId) {
    id
    name
  }
}

query GetAllProvincias {
  provincias {
    nodes {
      id
      nome
    }
  }
}
